
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <link rel="stylesheet" href="@Href("~/Site.css")" type="text/css" />
        <title>Home</title>
    </head>
    <body>
        <h1>Home Page</h1>
        <p>This is the home page.</p>
        <p> <a href="@Href("~/SexPredict")">Sex Predict</a></p>
        <p>
          @if(WebSecurity.IsAuthenticated){
            @:<p>Welcome, @WebSecurity.CurrentUserName</p>
            @:<p><a href="@Href("~/Account/Logout")">Log out</a></p>
       
          }
          else{
            @:<a href="@Href("~/Account/Login")">Log in</a>
            @:<a href="@Href("~/Account/Register")">Register</a>
          }
         
        </p>
        
      <p>
        <a href="@Href("~/Members/BabyActualSex")">Get the accuracy percentage</a>
      </p>
 
    </body>
</html>